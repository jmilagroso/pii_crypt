language: python
python:
  - "3.6" # current default Python on Travis CI
  - "3.7"
install:
  - npm install snyk
  - pip install codecov
  - pip install pycryptodome
script:
  - snyk auth
  - coverage run -m unittest discover
after_success:
  - codecov
  - snyk monitor
